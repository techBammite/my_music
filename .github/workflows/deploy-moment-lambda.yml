name: Deploy Moment Microservice (AWS Lambda)

on:
  push:
    branches:
      - main
    paths:
      - 'src/service_auxiliere/moment/**'
      - 'infra/moment/**'
      - '.github/workflows/deploy-moment-lambda.yml'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy-moment:
    name: Deploy Moment Service to AWS Lambda
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Moment Microservice Dependencies
        run: |
          cd src/service_auxiliere/moment
          npm install --production

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION || 'eu-west-3' }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.5

      - name: Terraform Init
        run: terraform init -input=false
        working-directory: ./infra/moment

      - name: Terraform Validate
        run: terraform validate
        working-directory: ./infra/moment

      - name: Terraform Apply
        id: apply
        run: terraform apply -auto-approve
        working-directory: ./infra/moment

      - name: Display Moment Service Live URL
        run: |
          URL=$(cd infra/moment && terraform output -raw lambda_function_url)
          echo "=========================================================="
          echo "🎉 MOMENT LAMBDA SERVICE DEPLOYED SUCCESSFULLY!"
          echo "🌐 Live Public URL: $URL"
          echo "=========================================================="
          echo "### 🚀 Moment Microservice Deployed to AWS Lambda!" >> $GITHUB_STEP_SUMMARY
          echo "Public Endpoint: **[$URL]($URL)**" >> $GITHUB_STEP_SUMMARY
